__pycache__/
*.pyc
.venv/
venv/
# Only the runtime archive at the repo root — NOT app/data/, which ships with the app.
/data/
*.db
*.sqlite
*.sqlite3
groq key.txt
Test-Mio-CV/
lavori_webapp_*.csv
*.log
stitch
node_modules/
playwright-report/
test-results/
screenshots/readme/*.json
.pytest_cache/
.coverage
htmlcov/
local_secrets.json
.playwright-mcp/
CLAUDE.md
phase*.png
verify*.png
snapshot-*.md
*.stackdump
.env
.env.local
.mypy_cache/
.ruff_cache/
coverage.xml
coverage.json
*.egg-info/
dist/
build/
tests/e2e/_demo_frames/
HANDOFF.md
APPUNTI.md
CANDIDATURE.md
# Knowledge graph: rebuilt locally with `graphify . --update`, and it changes on
# every build. Anchored to the repo root so it cannot match a `graphify-out` a
# source folder might one day hold — the same slip that hid app/data/ for months.
/graphify-out/
